
// const button = document.querySelector(".buttonTest");
// button.addEventListener("click", () => alert("buttonclicked"));

const buttons = [...document.querySelectorAll(".button")];

const signs = [...document.querySelectorAll(".button")].filter((id) => id.id.includes("Sign"));

